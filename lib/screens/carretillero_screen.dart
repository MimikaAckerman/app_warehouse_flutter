import 'package:flutter/material.dart';

class CarretilleroScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Carretillero")),
      body: Center(child: Text("Pantalla de Carretillero")),
    );
  }
}
